;
; Call Features Configuration
;

[general]
; Parking extension settings (disabled by default since res_parking is noloaded)
; parkext => 700
; parkpos => 701-720

; Transfer digit timeout
transferdigittimeout => 3

; Feature digit timeout
featuredigittimeout => 1000

; Attended transfer is *2
atxfer => *2

; Blind transfer is ##
blindxfer => ##

[featuremap]
; DTMF features during calls

[applicationmap]
; Custom applications triggered by DTMF
